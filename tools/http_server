#! /usr/bin/env ruby
# frozen_string_literal: true

require 'webrick'
port = (ARGV[0] || 8080).to_i
WEBrick::HTTPServer.new(
  DocumentRoot: './',
  Port: port,
  MimeTypes: {
    'md' => 'text/markdown',
    'adoc' => 'text/asciidoc',
  },
).start
